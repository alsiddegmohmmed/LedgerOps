# Requirement traceability — Releases 0.1 and 0.2

Authority: approved LedgerOps Product Definition v1.6 and approved LedgerOps Technical Design and Architecture Specification v1.6. Accepted decisions include retrospective ADR-004, ADR-005, ADR-006, and ADR-009, plus ADR-016 through ADR-021.

This matrix records derived implementation evidence. It does not modify either authoritative document.

This matrix tracks requirements touched by Release 0.1 and implemented or planned for Release 0.2. Product requirements not listed here remain part of the authoritative Product Definition and are not silently removed.

Status values: `Implemented`, `Partial`, `Planned`, `Deferred`, or `Blocked`.

| Requirement | Release 0.1 interpretation | Evidence | Status |
|---|---|---|---|
| TEN-01 | Release 0.1 implements tenant identity data and lifecycle mechanics only. The designated initial Merchant Admin, authenticated platform-administrator action, membership/role assignment, and authorised historical access require the Identity and authorization work scheduled by the Technical Specification for Release 0.3. | Current: domain lifecycle tests, Flyway schema, persistence adapter, transactional application use cases, typed failures, validated HTTP/OpenAPI lifecycle contract, stable correlated RFC 7807 failures, safe logging, and PostgreSQL-backed HTTP/application tests. Remaining: Merchant and Customer write use cases must enforce suspended-tenant blocking when introduced; Release 0.3 supplies initial-admin designation and authorization evidence. | Partial |
| PAY-01 | create a stable tenant/merchant-owned payment with merchant reference, amount, currency, customer identifier, payment-method category, and idempotency key. Authorization is completed with the Release 0.3 identity boundary. | Current: domain aggregate, PostgreSQL schema/store, published ownership/activity checks, validated HTTP/OpenAPI creation contract, stable identifier and `CREATED` response, RFC 7807 failures, structured logs, and Testcontainers evidence cover every request field. Remaining: Release 0.3 authentication and authorization. | Partial |
| PAY-02 | The Payment API idempotency namespace is tenant-wide. Equivalent requests under the same `tenantId + idempotencyKey` return the original logical result. Materially different content, including a different merchant, returns an explicit idempotency conflict. | Unique `(tenant_id, idempotency_key)` constraint, merchant-aware SHA-256 request fingerprint, atomic PostgreSQL insert-or-find, sequential replay, changed-content conflict, cross-merchant conflict, cross-tenant independence, coordinated eight-request concurrency, HTTP, and final-database-state tests pass. | Implemented |
| PAY-03 | only the ADR-016 Payment transitions may occur; `REJECTED`, `FAILED`, and `REVERSED` are terminal, and provider, Reversal, and Reconciliation progress remain separate dimensions | Exact vocabulary and approved transitions have executable evidence. Release 0.1 Slice 8 implements ADR-020 completion. Release 0.2 Slices 1, 4, and 5 implement immutable Payment Attempts, `APPROVED -> PROCESSING`, exact Provider-result handling, `PROCESSING -> FAILED`, Provider-evidence-gated ADR-020 completion, and retry/recovery without another Payment status. Reversal remains later work. | Partial |
| PAY-06 | one authorised full-payment Reversal workflow may be requested per `COMPLETED` Payment; failed retries remain in that workflow and add immutable provider attempts | Documentation remains aligned in Product v1.6 §6.6/PAY-06, Technical v1.6 §5.4, and ADR-016. Reversal implementation remains scheduled for Release 0.3. | Deferred |
| PAY-08 | expose `REQUESTED`, `PROCESSING`, `FAILED`, and `COMPLETED` Reversal history; only completed reversal atomically compensates the ledger and changes Payment to `REVERSED` | Documentation remains aligned in Product v1.6 PAY-08/§8.4, Technical v1.6 §5.4, and ADR-016. Reversal implementation remains scheduled for Release 0.3. | Deferred |
| RSK-01 | Evaluate every eligible Payment synchronously against all enabled `PAYMENT_AMOUNT_THRESHOLD` rules for its currency. Record the exact profile version, every eligible rule result, uncapped and final scores, decision, and evaluation time. | Domain, application, migration, persistence, rollback, repeat, and coordinated-concurrency tests verify the exact rule model, versioned evidence, published boundary, one evaluation, and atomic Payment-owned orchestration. | Implemented |
| RSK-02 | Produce `APPROVE`, `MANUAL_REVIEW`, or `REJECT` from an integer score capped at 100 and tenant thresholds satisfying `1 <= reviewThreshold < rejectThreshold <= 100`; map the result only to the approved Payment transitions from `VALIDATING`. | Domain, application, and PostgreSQL tests verify invalid threshold ordering, exact score boundaries, all three decisions, and exact Payment transition mapping. Manual-review queues and human decisions remain outside Release 0.1. | Implemented |
| LED-01 | balanced journal transaction with at least one debit and credit; accepted ADR-020 defines the Payment-success template as exactly full-amount `DEBIT PROVIDER_CLEARING` and `CREDIT MERCHANT_PAYABLE` in the Payment currency | General Ledger invariants, V7 atomic persistence, and Slice 8 Payment-success template, rollback, consistency-matrix, replay, and concurrency evidence pass. Other later financial workflows remain. | Partial |
| LED-02 | maintain tenant-scoped accounts with immutable identity and currency, exactly `ACTIVE` status, deterministic tenant/code/currency identity, the six approved account codes, and readable history derived from immutable entries | The account domain, exact catalog, create-only repository, V6 schema, V7 posting schema/service, tenant-scoped validation, constraints, derived-history queries, and PostgreSQL tests pass. The aggregate contains no unbounded history collection. | Implemented |
| LED-03 | Ledger posting links to its source; Payment-success identity is exactly `tenantId + PAYMENT + paymentId`, and replay requires the exact posting shape rather than source existence alone | Domain and V7 persistence require source category and identifier; `(tenant_id, source_type, source_id)` prevents duplicates. Slice 8 implements tenant/source lookup and verifies every field of the existing Payment-success posting before replay. Other source workflows and bidirectional operational navigation remain. | Partial |
| LED-04 | posted journal transactions and entries cannot be edited or deleted; corrections create new transactions with explicit original references | Domain objects are immutable, compensations require a distinct original transaction, same-tenant compensation references use a database foreign key, and PostgreSQL triggers reject transaction/entry mutation or deletion. The authorised correction workflow remains. | Partial |
| LED-05 | authorised users can view account balances and date-bounded statements whose totals reconcile to the selected immutable entries and currency | Tenant-scoped application queries return currency-scaled debit and credit totals and immutable entry evidence using explicit half-open time bounds, bounded stable pagination, and one PostgreSQL snapshot. HTTP presentation and Release 0.3 authorization remain. | Partial |
| BR-01 | every business-owned record belongs to one tenant | Tenancy, Merchant, Customer, Payment, Risk, and all current Ledger records have non-null tenant ownership, scoped persistence, and PostgreSQL isolation evidence. Later records remain subject to the same invariant. | Partial |
| BR-02 | money has explicit currency and decimal precision appropriate to that currency | Payment-module `Money` uses `BigDecimal`, explicit `Currency`, currency-defined precision, non-negative values, and same-currency arithmetic tests. Payment, ledger, and reversal adoption remains. | Partial |
| BR-03 | correct financial history using new records | Immutable Ledger domain objects, append-only PostgreSQL transactions/entries, and same-tenant compensation references pass; the authorised correction workflow remains. | Partial |
| BR-04 | one tenant and idempotency key identifies one logical payment; `merchantId` is not part of the uniqueness boundary, and reuse with a different merchant is conflicting content rather than a second payment | unique `(tenant_id, idempotency_key)` constraint, merchant-aware request fingerprint, cross-merchant conflict, cross-tenant independence, and coordinated concurrent final-state evidence pass | Implemented |
| BR-10 | suspended tenants retain history but cannot create activity | `Tenant.canCreateOperationalActivity()` and published activity checks exist; Payment creation and replay tests prove new activity is blocked while an existing idempotent result remains readable. Merchant and Customer application write enforcement remains. | Partial |
| BR-12 | time is consistent and displayed with locale/timezone context | UTC `Clock` bean exists; broader persistence/API evidence remains | Partial |
| BR-13 | Payment, Reversal, Reconciliation, and provider-attempt progress are separate dimensions | Authoritative documentation and ADR-016 are aligned. Executable Payment lifecycle, Risk, atomic completion, Payment Attempt, Provider work/evidence, and exact Provider-result application evidence pass without adding a Payment status. Reversal and Reconciliation remain deferred to their scheduled releases. | Partial |
| BR-14 | only a completed full Reversal changes Payment from `COMPLETED` to `REVERSED`; partial and cumulative reversals are prohibited | Authoritative documentation and ADR-016 are aligned; atomic reversal and prohibition tests remain scheduled for Release 0.3. | Deferred |

## Release 0.2 evidence

Slices 0 through 7 are complete. The matrix preserves the approved Release 0.2 exit statuses: product requirements remain `Partial` or `Deferred or Planned` where later product work is still required, while BR-05 and BR-06 are `Implemented` because their complete executable evidence passes.

| Requirement | Release 0.2 interpretation | Planned evidence | Current and release-exit status |
|---|---|---|---|
| PAY-03 | Implement the automated Provider lifecycle path while preserving the approved Payment state machine and ADR-020 completion. | Slices 1, 4, and 5: exact submission, immutable attempts, result matrix, accepted-final evidence, definitive failure, evidence-gated ADR-020 completion, safe retry, status recovery, rollback, replay, conflict, and concurrency tests. | Slices 1, 4, and 5 implemented; release exit `Partial` |
| PAY-04 | Persist Payment Attempt and Provider evidence for future composed detail views without delivering the complete product-facing view. | Slices 1, 3, 4, and 7: immutable Payment Attempt, tenant-owned Provider interaction/result evidence, published evidence query, Payment-owned accepted-final evidence, runbook navigation, and synthetic demonstration evidence. The product-facing composed view remains later work. | Release 0.2 evidence complete; release exit `Partial` |
| PRV-01 | Provide deterministic Provider Simulator scenarios without the later authenticated administration workflow. | Slices 3, 6, and 7: separate Simulator database, test/seed-owned deterministic outcomes, signed submission/status APIs, durable idempotency, timeout recovery, duplicate, delayed, missing, out-of-order, invalid-signature, and conflicting webhook scenarios, plus the local demo. | Release 0.2 evidence complete; release exit `Partial` |
| PRV-02 | Implement immutable Payment-associated attempts and separate Provider-owned interaction/result evidence. Reversal attempts remain Release 0.3. | Slices 1, 3, 4, and 5: immutable Payment-associated attempts including safe retries, append-only Provider interactions/results, exact accepted-final application evidence, and separate retry-request evidence. | Slices 1, 3, 4, and 5 implemented; release exit `Partial` |
| PRV-03 | Durably receive and asynchronously process signed Provider webhooks while preserving duplicate, invalid, out-of-order, and conflicting evidence without duplicate effects. | Slice 6: bounded signed HTTP reception, exact authentication/attribution outcomes, Provider-owned identity mapping, immutable receipt/conflict evidence, fenced asynchronous processing, Stage A result publication, Kafka Stage B application, and one-effect end-to-end tests. Product timeline UI remains later. | Implemented in Release 0.2 scope; release exit `Partial` |
| PRV-04 | Implement bounded automatic safe retry and status recovery. Public manual retry remains deferred until Release 0.3 authorization and audit. | Slice 5: exact dispositions, deterministic bounded schedules, fenced retry requests, authoritative evidence verification, maximum three attempts and 12 status queries, durable exhaustion, PostgreSQL/Kafka/concurrency evidence. | Implemented in Release 0.2 scope; release exit `Partial` |
| PRV-05 | Expose Provider health through bounded Prometheus metrics and initial Grafana dashboards. These dashboards do not complete the product-facing operations experience. | Slice 7: exact bounded metrics, two provisioned Grafana dashboards, eight Prometheus alerts, and linked operations procedures. | Release 0.2 evidence complete; release exit `Partial` |
| DEV-01 | Add signed Provider and event-contract developer reference material. | Slices 1–3 and 7: versioned command/result JSON Schemas, valid/invalid contract evidence, shared HMAC golden fixtures, Provider HTTP reference, Provider-flow diagram, and local topology instructions. | Release 0.2 evidence complete; release exit `Partial` |
| DEV-02 | Merchant webhook testing is not implemented by Provider Simulator-to-Core webhooks. | Outside Release 0.2 implementation scope. | Deferred or Planned |
| DEV-03 | Add deterministic distributed-processing failure stories without an authenticated scenario launcher. | Slices 3–7 verify success, decline, temporary failure, idempotency conflict, timeout-after-acceptance, status recovery, duplicate delivery, poison messages, conflicting results, signed webhook failures, and durable exhaustion. | Release 0.2 evidence complete; release exit `Partial` |
| DEV-04 | Add synthetic Provider, attempt, messaging, and failure evidence to the demo. | Slice 7 supplies the synthetic Release 0.2 demo, separate-database topology, observability inspection, expected evidence, and safe reset instructions. | Release 0.2 evidence complete; release exit `Partial` |
| BR-01 | Enforce tenant ownership for mapped Core business records and isolate unattributed webhook-security evidence from business effects. | Slices 1–6: non-null tenant ownership and isolation for attempts, messaging, Provider work/interactions/results, inbox processing, accepted-final evidence, mapped webhook receipts/events, and webhook work. Platform rejection and authenticated-unmapped evidence cannot create tenant work. | Implemented through Release 0.2 Slice 6; release exit `Partial` |
| BR-05 | Duplicate commands, Provider results, status observations, and webhooks produce one accepted business and financial effect. | Slices 1–7: stable identities, inbox deduplication, one durable work item/result message, one accepted final result, exact lifecycle replay, conflict preservation, duplicate webhook evidence, one ADR-020 financial effect under HTTP/Kafka redelivery and concurrency, and the clean release gate. | Implemented |
| BR-06 | Automatic submission retry and status recovery follow the exact ADR-021 dispositions and limits; ambiguity or exhaustion never invents a final failure. | Slices 3 and 5 enforce exact dispositions, durable no-acceptance proof, deterministic bounded scheduling, status-query-only recovery, maximum attempts, authoritative Payment verification, and durable `UNRESOLVED` exhaustion while Payment remains `PROCESSING`. | Implemented |
| BR-12 | Use injected time for leases, retries, signatures, evidence, and deterministic tests. | Slices 1–7 use injected `Clock` for evidence, leases, HMAC timestamps and replay windows, failure timestamps, backlog age, retry due times, status-recovery schedules, webhook processing, and stuck-Payment measurement. | Release 0.2 evidence complete; release exit `Partial` |
| BR-13 | Keep Provider progress separate from Payment, Reversal, and Reconciliation status. | Slices 1–7 store attempts, messaging, Provider work, interactions, result categories, dispositions, recovery state, retry requests, webhook state, accepted-final evidence, and operational measures outside `PaymentStatus`; recovery leaves Payment `PROCESSING`. | Release 0.2 evidence complete; release exit `Partial` |

## Technical decision evidence

| Technical section | Decision | Evidence/status |
|---|---|---|
| ADR-004; Technical §§7.4–7.5 | Business state and outbound-message intent commit atomically through a transactional outbox. | Slice 1 commits Payment submission and outbox intent atomically; Slice 2 publishes through short fenced claims outside the business transaction. |
| ADR-005; Technical §§7.4, 7.6–7.7 | At-least-once delivery uses idempotent consumers and makes no end-to-end exactly-once claim. | Slice 2 implements at-least-once command publication, inbox deduplication, durable failure/dead-letter handling, and duplicate-publication recovery. Later consumers must retain the same model. |
| ADR-006; Technical §§7.2–7.3 | Kafka topics follow business capabilities and use aggregate partition keys. | Slice 2 publishes `SubmitPaymentToProvider` to `ledgerops.provider.commands.v1` with Payment ID as the partition key and verifies delivery through Kafka Testcontainers. |
| ADR-009; Technical §§3, 9.1, 9.6, and 12.2 | Provider Simulator is a separate deployable application with its own database and no Core database access. | Slice 3 adds `applications/provider-simulator`, a Simulator-owned Flyway schema, and independent PostgreSQL Testcontainers evidence. |
| ADR-020 and ADR-021; Technical §§5.6, 9.2–9.3, 13, 14.2, and 17 | Release 0.2 Stage B SUCCESS preserves the existing Payment-owned ADR-020 completion transaction and exact Ledger replay verification. | Slice 4 invokes the unchanged completion use case inside the inbox/accepted-result/lifecycle-outbox transaction and proves exact posting, replay, rollback, and concurrency. |
| ADR-021; Technical §§4–7, 9, 11, 13, 14.2, and 17 | Exact Release 0.2 module graph, identities, outbox/inbox, leases, Provider work, retry, HMAC, webhook, resilience, observability, scope, and verification semantics. | Slices 1–7 implement and verify submission, Kafka delivery, fenced work, signed Provider HTTP and webhooks, separate Simulator persistence, immutable Provider and webhook evidence, evidence-gated Payment result application, bounded retry/recovery, exact dead-letter behavior, OpenTelemetry propagation, Prometheus, two Grafana dashboards, migrations, runbooks, and the release gate. |
| §4.1–4.4 | modular monolith, internal layering, owned schemas, no cross-table access | Tenancy and Merchant module declarations, published tenancy API, restricted Merchant dependency, separate schemas, and Modulith verification exist; expand with each module |
| §4.3 and §5.1 | Merchant owns profiles/state/configuration/limits, belongs to exactly one tenant, and does not live inside the Tenant aggregate | merchant identity/state foundation, mandatory tenant reference, owned schema, scoped repository, and isolation tests implemented; configuration and limits remain planned |
| §4.3, §5.1, and §8.6 | Customer owns simulated references, status, merchant relationship, risk attributes, and tokenized references while storing no real card credentials or unnecessary personal profiles | customer identity/reference/status foundation, mandatory tenant/merchant ownership, data-minimized owned schema, scoped repository, and isolation tests implemented; risk and tokenized attributes remain planned |
| §4.3 and §14.3 | Identity owns users, membership references, and role assignments; Release 0.3 introduces Keycloak, identity, tenant membership, permissions, merchant scope, authorization, and tenant-isolation enforcement | TEN-01 remains Partial in Release 0.1; Tenancy does not absorb identity-owned data |
| §5.2 | explicit value objects, `BigDecimal` Money with currency, same-currency arithmetic, non-negative values, and injected time | `TenantId`, `MerchantId`, Customer identifiers/references, `PaymentId`, `IdempotencyKey`, payment-method category, `Clock`, and Payment-module `Money` with invariant tests implemented |
| Product §8.2, BR-13; Technical §§5.3–5.4; ADR-016 | exact Payment and Reversal lifecycles with separate provider-attempt and Reconciliation dimensions | Exact Payment status/transition tests and PostgreSQL-backed Release 0.1 validation/Risk orchestration pass. Reversal, provider-attempt, and Reconciliation implementation remain later-release work. |
| Technical §§4.3, 5.1, 9.2–9.5; ADR-016 | Payment owns immutable Payment Attempt business records and accepted-final application evidence; Provider owns adapters, communication evidence, retries, ambiguity, and recovery orchestration | Slice 1 implements Payment Attempt ownership. Slice 3 implements Provider-owned work, HTTP adapter, immutable interaction/result evidence, and the published neutral evidence query. Slice 4 implements Payment-owned evidence-gated result application. Slice 5 implements Provider-owned scheduling and retry requests plus Payment-owned creation of the next attempt through the acyclic command flow. Slice 6 implements Provider-owned webhook attribution, evidence, and fenced processing without Payment-table access. |
| Product sensitive-action baseline; Technical §§8.3, 8.5, 13, 14.3, 17; ADR-016 | tenant-scoped capability, explicit confirmation, reason where applicable, immutable audit, and re-authentication/MFA where required; formal approval chains deferred | Documentation reconciled; security implementation remains scheduled by the release plan. |
| Product LED-02; Technical §§4.3, 5.1, 5.5, 6.2, and 13; ADR-019 | Release 0.1 Ledger accounts use exactly `ACTIVE`, the six approved account codes, immutable account fields, unique `(tenant_id, account_code, currency)`, history derived from immutable entries, and atomic posting rejection for missing, cross-tenant, currency-mismatched, or non-ACTIVE accounts | The account domain, repository, V6/V7 schemas, posting service, validation failures, constraints, rollback, derived-history queries, and PostgreSQL/Testcontainers evidence pass. |
| §5.5 | strict, single-currency double-entry ledger | Journal/account invariants, atomic persistence, duplicate-source prevention, relationships, append-only triggers, deferred database balance verification, tenant-scoped balance/statement queries, and the exact Payment-success posting pass. |
| ADR-020; Technical §§4.3, 5.1, 5.5–5.7, 6.2, 9.2–9.3, 13, 14, and 17.2 | Payment owns one joined PostgreSQL completion transaction; Ledger owns exact template construction, account/source lookup, validation, and persistence behind `ledger::api`; every replay validates the complete two-entry posting and every inconsistent state raises a typed error | Implemented through `CompletePaymentAfterProviderSuccess`, the neutral published Ledger API, tenant-scoped Payment locking, source lookup, exact replay verification, typed consistency errors, and PostgreSQL rollback/concurrency tests. |
| §5.6 | atomic Payment completion and exact Ledger posting | Implemented: one transaction posts full-amount `DEBIT PROVIDER_CLEARING` and `CREDIT MERCHANT_PAYABLE`, then changes Payment from `PROCESSING` to `COMPLETED`; replay and every inconsistent state/posting branch are verified. |
| Product PAY-02 and BR-04; Technical §5.7; ADR-017 | Payment API idempotency uses exactly `tenantId + idempotencyKey`; `merchantId` remains request content and is excluded from the uniqueness boundary | `Clock`, unique `(tenant_id, idempotency_key)` constraint, merchant-aware request fingerprint, atomic insert-or-find, replay, conflict, and coordinated concurrency evidence implemented |
| Product RSK-01 and RSK-02; Technical §§4.3, 5.1, 5.2, and 5.8; ADR-018 | Release 0.1 uses only `PAYMENT_AMOUNT_THRESHOLD`, versioned tenant profiles, integer scoring capped at 100, exact decision boundaries, append-only evidence, two Payment-owned transactions, optimistic concurrency, and one initial evaluation per Payment | Domain scoring, published `risk::api`, typed errors, V5 schema, Risk JDBC persistence, replay behavior, both Payment-owned transactions, exact mapping, rollback, and coordinated-concurrency evidence pass. |
| §14.3–14.4 | Release 0.3 introduces identity and authorization enforcement; Release 1.0 completes security hardening and release evidence | release descriptions aligned; implementation remains scheduled by the roadmap |
| §6.1–6.4 | one PostgreSQL database, module schemas, Flyway ownership | Tenancy, Merchant, Customer, Payment, Risk, and Ledger use module-owned Flyway schemas with passing migration and constraint tests. Ledger accounts, transactions, and entries use only same-schema relationships. |
| §13 | layered tests and critical verification | Domain, application, HTTP/OpenAPI contract, PostgreSQL, rollback, concurrency, Modulith, domain-isolation, published-API, error-safety, health, operational-logging, and deterministic-demo evidence passes through Slice 9. Slice 8 supplies the exact posting/replay and joined-transaction evidence. |
| §14.1 | modular/PostgreSQL foundation; tenant/merchant/customer basics; payment, idempotency, state machine, synchronous risk; strict ledger and atomic completion; OpenAPI, structured logs, and tests | Release 0.1 is complete and mapped in `docs/plans/release-0.1-transactional-core.md`; partial product requirements remain explicitly partial here. |
| Product DEV-01; Technical §§7, 11, 12, 13, and 14.1 | developers can discover the Release 0.1 HTTP contract, examples, idempotency and error semantics, run a local sandbox, correlate failures, and verify architecture and operational evidence | Versioned OpenAPI with executable controller alignment; stable safe RFC 7807 problems; `X-Correlation-Id`; correlation-aware logs without payload data; health smoke test; idempotent synthetic seed; and `docs/demo/release-0.1.md`. Authentication is explicitly absent from the local Release 0.1 sandbox and remains scheduled for Release 0.3. |

## Updating this matrix

Mark a requirement `Implemented` only when its complete acceptance behaviour has executable evidence. A class or happy-path test alone is not enough. Every implementation pull request must update this matrix or explain why no mapped requirement changed.
