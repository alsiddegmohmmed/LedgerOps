{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ledgerops.dev/contracts/v1/ReversalRequested.schema.json",
  "title": "ReversalRequested v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["reversalId", "tenantId", "paymentId", "merchantId", "status", "amount", "currency", "aggregateVersion", "requestedAt"],
  "properties": {
    "reversalId": { "type": "string", "format": "uuid" },
    "tenantId": { "type": "string", "format": "uuid" },
    "paymentId": { "type": "string", "format": "uuid" },
    "merchantId": { "type": "string", "format": "uuid" },
    "status": { "const": "REQUESTED" },
    "amount": { "type": "string", "pattern": "^(0|[1-9][0-9]*)(\\.[0-9]+)?$" },
    "currency": { "type": "string", "pattern": "^[A-Z]{3}$" },
    "aggregateVersion": { "type": "integer", "minimum": 0 },
    "requestedAt": { "type": "string", "format": "date-time" }
  }
}
